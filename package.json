{
  "name": "googlebusinessdata",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "app",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=app",
    "build:app": "npm run build --workspace=app",
    "build:types": "npm run build --workspace=packages/shared-types",
    "build": "npm run build:types && npm run build:app",
    "type-check": "npm run type-check --workspace=app",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^2.1.0"
  }
}
